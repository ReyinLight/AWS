name: Test Runner

on:
  push:
    branches: [ test-runner ]

jobs:
  test:
    runs-on: synology-runner
    steps:
      - name: Hola desde Synology
        run: |
          echo "¡El runner funciona!"
          echo "Estoy corriendo en: $(hostname)"
          docker --version